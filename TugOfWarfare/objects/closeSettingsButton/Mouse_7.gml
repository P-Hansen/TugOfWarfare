/// @description Insert description here


settings.windowOpen = false;

instance_destroy(settings.volumeButton);
instance_destroy(settings.fullScreenButton);
instance_destroy(settings.gameRestart);

instance_destroy(settings.resbutton1);
instance_destroy(settings.resbutton2);
instance_destroy(settings.resbutton3);

instance_destroy(self);